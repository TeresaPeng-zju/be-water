"use client";import { ArrowRight,Plus } from "lucide-react";import Link from "next/link";import { useLocalCases } from "./use-local-cases";
export function CaseList(){const cases=useLocalCases();return <main className="min-h-dvh lg:ml-[224px]"><div className="mx-auto max-w-[1040px] px-5 pb-14 pt-20 sm:px-8 lg:px-14 lg:pt-24"><header className="flex items-end justify-between gap-6"><div><p className="text-xs font-medium tracking-[.12em] text-[var(--brand)]">过程连成水脉</p><h1 className="page-title mt-3">案例</h1><p className="mt-3 text-sm text-[var(--muted)]">每一个案例，都是一段完整服务过程。</p></div><Link href="/workspace/cases/new" className="inline-flex h-11 items-center gap-2 rounded-xl bg-[var(--brand)] px-4 text-sm font-semibold text-white"><Plus className="size-4"/>新建案例</Link></header>{cases.length?<div className="mt-12 space-y-3">{cases.map(item=><Link key={item.id} href={`/workspace/cases/${item.id}`} className="group grid gap-4 rounded-[20px] bg-white/56 px-6 py-6 transition hover:-translate-y-0.5 hover:bg-white/78 sm:grid-cols-[1fr_180px_28px]"><div><p className="text-xs font-medium text-[var(--brand)]">{stageLabel(item.stage)}</p><h2 className="mt-2 text-xl font-semibold">{item.customerName} · {item.serviceName}</h2><p className="mt-2 text-sm text-[var(--muted)]">下一步：{item.nextAction}</p></div><div className="self-center"><span className="block h-px bg-[linear-gradient(90deg,#7fa8b5,transparent)]"/><p className="mt-2 text-xs text-[var(--subtle)]">{item.events.length} 条真实记录</p></div><ArrowRight className="self-center text-[var(--subtle)] transition group-hover:translate-x-1"/></Link>)}</div>:<section className="mx-auto mt-20 max-w-[650px] rounded-[28px] bg-white/50 p-10 text-center"><h2 className="text-[28px] font-semibold">还没有案例</h2><p className="mx-auto mt-4 max-w-[500px] text-sm leading-7 text-[var(--muted)]">从一项服务开始，让每次真实服务继承清晰的价格、交期和标准流程。</p><Link href="/workspace/cases/new" className="mt-8 inline-flex h-11 items-center gap-2 rounded-xl bg-[var(--brand)] px-5 text-sm font-semibold text-white"><Plus className="size-4"/>新建案例</Link></section>}</div></main>}
export function stageLabel(stage:string){return ({inquiry:"进行中",requirement_confirmation:"需求确认",quoted:"待成交",won:"已成交",delivery:"交付中",feedback:"待反馈",completed:"已完成"} as Record<string,string>)[stage]??"进行中"}
