import {CaseDetailPage} from "@/components/business-memory/case-detail-page";
export default async function CaseRoute({params,searchParams}: {params: Promise<{serviceId: string; caseId: string}>;searchParams:Promise<{from?:string}>}) { const [{serviceId, caseId},{from}] = await Promise.all([params,searchParams]); return <CaseDetailPage serviceId={serviceId} caseId={caseId} returnTo={from === "growth" ? "growth" : "service"}/>; }
